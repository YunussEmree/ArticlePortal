import { WriterfilterPipe } from './writerfilter.pipe';

describe('WriterfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WriterfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
